CREATE TABLE [dbo].[FieldDrawingIntegratedSystem] (
    [Id]     INT           IDENTITY (1, 1) NOT NULL,
    [System] NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_FieldDrawingIntegratedSystem] PRIMARY KEY CLUSTERED ([Id] ASC)
);

